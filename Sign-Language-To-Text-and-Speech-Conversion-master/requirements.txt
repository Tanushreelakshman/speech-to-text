# Sign Language to Text and Speech Conversion - Required Packages

# Computer Vision and Image Processing
opencv-python==4.10.0.84
cvzone==1.6.1
Pillow==11.3.0
mediapipe==0.10.21
# MediaPipe compatibility note:
# - `mediapipe==0.10.20` ships prebuilt wheels for CPython 3.10, 3.11 and 3.12 on Windows.
# - If you are using Python 3.13 (your current venv), `pip` will fail with "No matching distribution".
# To install MediaPipe, create a venv using a supported interpreter (example uses Python 3.12):
#
#   winget install --id Python.Python.3.12 -e                # (optional) install Python 3.12 via winget
#   py -3.12 -m venv .venv312
#   .\.venv312\Scripts\Activate.ps1
#   python -m pip install --upgrade pip setuptools wheel
#   pip install mediapipe==0.10.20
#
# If you cannot install Python 3.12, you can continue development without MediaPipe
# by leaving this package in the file but installing other dependencies first.

# Machine Learning and Deep Learning
tensorflow==2.16.2
keras==3.3.3
numpy==1.26.4

# Text-to-Speech and Language Processing
pyttsx3==2.99
pyenchant==3.3.0

# Additional dependencies for mediapipe and visualization
matplotlib==3.10.6
sounddevice==0.5.2
attrs==25.3.0

# Note: tkinter is included with Python standard library and doesn't need to be installed separately